<div class="container container-sm mt-4">
  <div class="card">
    <div class="card-body">
      <h4 class="card-title">Edit password</h4>

      <%= form_with model: @user, url: users_password_path(@user), local: true do |f| %>
        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, disabled: true, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :password %> <%= required_mark %>
          <%= f.password_field :password, required: true, class: 'form-control' %>
          <p class="form-text small text-muted">
            Minimum is 8 characters
          </p>
        </div>

        <%= hidden_field_tag :token, @token %>

        <%= f.submit 'Update Password', class: 'btn btn-success' %>
      <% end %>
    </div>
  </div>
</div>
